#include "main.h"

/**
 * mul - check the multiple of two integers
 * @a and @b are the inputs
 * Description: a * b
 * Return: 0 Always
*/
int mul(int a, int b)
{
	int multiple;

	multiple = a * b;
	return (multiple);
}
